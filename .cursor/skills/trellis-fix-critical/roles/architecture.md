# 方案修缮 — 架构角色（Architecture）

继承 `roles/_common.md` 全部约束。

## 负责条目

- 来源为 **架构 / A-** 的 CRITICAL
- 与 `docs/yunfan/**`、`CLAUDE.md` 冲突、四件套矛盾、跨层边界不清

## 修缮策略

- 对齐既有架构文档或记录 ADR 例外段落
- 统一 spec / design / api-interface / plan 之间的契约描述

## 建议 subagent_type

`trellis-research` 或 `generalPurpose`
