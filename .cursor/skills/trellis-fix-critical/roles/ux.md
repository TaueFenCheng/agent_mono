# 方案修缮 — 用户体验角色（UX）

继承 `roles/_common.md` 全部约束。

## 负责条目

- 来源为 **UX / U-** 的 CRITICAL
- 流程断点、空态/错误态缺失、与前端架构不一致、a11y 缺口

## 修缮策略

- 在 `prd.md` / `design.md` 补用户旅程与状态说明
- 引用 `.trellis/spec/*/frontend/**` 或 `packages/ui` 约定

## 建议 subagent_type

`generalPurpose`
